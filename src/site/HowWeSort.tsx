import SmartSorting from "./SmartSorting";

/* "How We Sort" is now the Smart Sorting showcase built around the
   uploaded infographic. (The former zone-grid "Our Method" section and
   the video block were removed on request — the video slot can come
   back whenever the footage is ready.) */

export default function HowWeSort() {
  return <SmartSorting />;
}
